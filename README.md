# BuzzMini

Локальное распознавание речи «на лету»: нажал горячие клавиши — продиктовал — текст вставился в активное окно (буфер обмена + симуляция вставки). Работает через **faster-whisper** и **PyTorch**; при наличии NVIDIA GPU транскрипция идёт на видеокарте.

Идея — минимальный Push-to-talk без облака, в духе Buzz, но проще и под свой сценарий.

## Возможности

- Иконка в системном трее, запись по удержанию комбинации (по умолчанию связка с **правым Ctrl** + вторая клавиша — см. настройки и переменную `BUZZMINI_PTT_CHORD`).
- Выбор модели Whisper и загрузка с Hugging Face (в трее: **Models…**).
- Микрофон и аккорд PTT в **Settings**.
- Диалог **Donate — Донат** в меню трея — поддержка проекта и ссылка на Donatty (см. ниже).

## Требования

- **Python 3.12–3.14**
- Windows / Linux / macOS (GPU на Windows чаще всего через CUDA-сборку PyTorch).
- Для GPU: драйвер NVIDIA и установленный **CUDA-сборный** `torch` в том окружении, из которого вы запускаете приложение. Системный `python` без CUDA даст работу на **CPU** (медленнее).

## Установка и запуск

Рекомендуется виртуальное окружение в корне репозитория.

### Через uv

```bash
cd BuzzMini
uv sync
uv run buzz-mini
```

### Через pip

```bash
cd BuzzMini
python -m venv .venv
.venv\Scripts\activate
pip install -U pip
pip install -e .
```

На **Windows** для GPU обычно нужно ставить PyTorch с CUDA отдельно (индекс совпадает с настройками в `pyproject.toml`, например cu126):

```bat
pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu126
pip install -e .
```

### Запуск приложения

Всегда используйте интерпретатор из того же venv, куда ставили зависимости:

```bat
cd BuzzMini
.\.venv\Scripts\python.exe -m buzz_mini.app
```

После `pip install -e .` можно также вызывать консольную команду `buzz-mini`, если активирован нужный venv.

### Где лежат модели

По умолчанию при запуске **из исходников** кэш моделей — папка **`models`** в корне репозитория (рядом с `pyproject.toml`). Иначе используется пользовательский кэш приложения и совместимость с каталогом Buzz.

Переопределение:

- `BUZZMINI_MODEL_ROOT` — свой каталог для весов и HF-снимков.

### Полезные переменные окружения

| Переменная | Назначение |
|------------|------------|
| `BUZZMINI_MODEL` | Модель по умолчанию (если не выбрано в настройках). |
| `BUZZMINI_LANGUAGE` | Язык распознавания, например `ru`. |
| `BUZZMINI_PTT_CHORD` | Строка аккорда, например `ctrl_r+win` или `ctrl_r+space`. |
| `BUZZMINI_FORCE_CPU` | Не `false` — принудительно CPU. |
| `BUZZMINI_DEVICE` | `cuda`, `cpu` или `auto`. |
| `BUZZMINI_PASTE_DELAY_MS` | Задержка перед симуляцией вставки после записи в буфер (мс). |
| `BUZZMINI_DONATE_URL` | URL страницы доната (перекрывает значение по умолчанию в диалоге «О донате»). |
| `BUZZMINI_LOG_LEVEL` | Уровень логирования, например `DEBUG`. |

## Smoke-тест без GUI

```bat
.\.venv\Scripts\python.exe tools\smoke_test.py
```

## Поддержать проект

Разработка ведётся в свободное время. Если BuzzMini оказался полезен, можно поддержать автора донатом — в приложении откройте в меню трея пункт **Donate — Донат**: откроется окно с текстом и кнопкой перехода на **Donatty**.

Пока в качестве универсальной ссылки используется заглушка `https://donatty.com`; когда будет готов профиль, автор заменит ссылку в коде или задаст переменную **`BUZZMINI_DONATE_URL`** для своей сборки.

Принимаются карты РФ, СБП и зарубежные карты — в рамках возможностей выбранной платёжной площадки.

## Публикация на GitHub (автору)

Репозиторий на GitHub можно назвать **`BuzzMini`** и сделать **Public** — так проще делиться ссылкой и принимать вклад, при этом секреты не храните в коде (токены, ключи API).

### Один раз на сайте GitHub

1. Войти на [github.com](https://github.com), **New repository**.
2. **Repository name:** `BuzzMini`.
3. **Description** — по желанию, до 350 символов, например: «Локальный push-to-talk: Whisper + PyTorch, вставка в активное окно без облака» или короткий EN-вариант для поиска.
4. **Public**.
5. **Add a README** — выключить (у вас уже есть `README.md` локально).
6. **Add .gitignore** — **No .gitignore** (или тот же шаблон, что у вас в репо; дублировать с сайта не нужно).
7. **Add license** — **выключить / No license**, если вы пушите уже готовый проект с файлами `LICENSE` и `NOTICE` из этого репозитория. Так не будет лишнего коммита на GitHub и конфликта с вашим первым `git push`. Если включить MIT на сайте, GitHub создаст репозиторий с одним коммитом «Initial commit» с чужим шаблоном `LICENSE`; тогда перед пушем придётся сделать `git pull` с merge/rebase и аккуратно оставить ваши `LICENSE` + `NOTICE`.

Скопируйте URL репозитория, например `https://github.com/ВАШ_ЛОГИН/BuzzMini.git`.

### У себя в консоли (в каталоге проекта)

Проверьте текущую ветку (`git branch`) и при необходимости переименуйте основную в привычную `main`:

```bat
git branch -M main
```

Привяжите удалённый репозиторий и отправьте код:

```bat
git remote add origin https://github.com/ВАШ_ЛОГИН/BuzzMini.git
git push -u origin main
```

Если вместо `main` вы ведёте работу в другой ветке (например `wip/hardening`), можно запушить её отдельно и потом открыть **Pull request** в `main` на GitHub, либо слить локально и запушить `main`.

Повторные выгрузки после коммитов:

```bat
git push
```

### Если GitHub предложит логин

Для HTTPS удобны **Personal Access Token** (Settings → Developer settings) вместо пароля, либо **GitHub CLI** (`gh auth login`), либо SSH-ключ и URL вида `git@github.com:ВАШ_ЛОГИН/BuzzMini.git`.

## Лицензия

Проект BuzzMini распространяется по лицензии **MIT** — см. файл [`LICENSE`](LICENSE).

В составе есть код, адаптированный из Buzz; для него действует отдельное уведомление и текст MIT автора Buzz — см. [`NOTICE`](NOTICE).
